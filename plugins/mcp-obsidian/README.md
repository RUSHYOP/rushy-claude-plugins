# mcp-obsidian

MCP-server plugin (packaged from RUSHYOP's system config, 2026-07-18).
Disabled by default — enable with `/plugin` -> mcp-obsidian@rushy per user/machine.

**Env:** OBSIDIAN_VAULT_PATH — absolute path to your Obsidian vault (required).
