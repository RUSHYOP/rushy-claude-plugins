# mcp-rapids

MCP-server plugin (packaged from RUSHYOP's system config, 2026-07-18).
Disabled by default — enable with `/plugin` -> mcp-rapids@rushy per user/machine.

**Env:** RAPIDS_MCP_TOKEN — bearer token (required). Ramco-internal endpoint (pearl.com network).
