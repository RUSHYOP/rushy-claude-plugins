# mcp-nebulastudio

MCP-server plugin (packaged from RUSHYOP's system config, 2026-07-18).
Disabled by default — enable with `/plugin` -> mcp-nebulastudio@rushy per user/machine.

**Env:** None (interactive auth). Ramco-internal endpoint.
